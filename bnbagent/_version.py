"""Single source of truth for the bnbagent SDK version.

Hatchling reads this file via [tool.hatch.version] in pyproject.toml,
so changing the literal below is the only thing needed to bump versions.
"""
__version__ = "0.3.3"
